# 測り方（2026-08-15 時点）

**公開している数字が、どういう条件で出たのかを1枚にする。**
コードと出力ファイルから拾い直した。記憶では書いていない。

いちばん大事なのは §4「揃っていないところ」。**聞かれる前にこちらから言う。**

---

## 1. 何を測っているか

**住民のAIが、自治体の手続きページを読んで、住民の問いに答えられるか。**

自治体を評価するものではない。**どこを直せば伝わるか**を示すためのもの。

見る4項目（+1）:

| 項目 | 意味 |
|---|---|
| 必要書類 | 持ち物が、条件つきで読み取れるか |
| 窓口/オンライン可否 | 窓口だけか、オンラインで完結するか |
| 期限 | 起算点つきで書かれているか |
| 手数料 | 額が書かれているか（無料なら「無料」と） |
| オンライン明示 | オンラインで完結するか否かが**迷わず**読み取れるか |

**ページに書かれていることだけから読み取る。推測での補完は禁止**（`extractor/prompt.md`）。

---

## 2. 手順（3段）

### ① 探索 — `crawler/discover.py`

自治体トップページから、**リンクだけを辿って**手続きページを探す。
**検索エンジンは使わない。**住民のAIがサイトの中で迷う様子を再現するため。

| | 値 | 場所 |
|---|---|---|
| 最大深さ | **3** | `MAX_DEPTH` |
| ビーム幅 | 深さ1: 親1・リンク6 / 深さ2: 親3・リンク4 / **深さ3: 親4・リンク3** | `BEAM` |
| 取得上限 | **26ページ** | `MAX_FETCHES` |
| 辿る範囲 | 既定は**同一ホストのみ**。`allow_subdomains` を立てた自治体だけ親ドメイン配下 | `link_filter()` |
| 減点 | English/中文/한국어/やさしい日本語/RSS/sitemap、PDFは −2 | `NEGATIVE_HINTS` |

**クロールの作法**（`crawler/polite_fetch.py`）:

- robots.txt を守る。**取得できないときは許可に倒さない**（500/503/通信失敗はすべて取得しない）
- 同一ドメインへは **3秒以上**あける。`Crawl-delay` がそれより長ければ従う
- User-Agent に**プロジェクト名と連絡先**を入れる — `TokyoAgentReadinessBot/0.1 (+https://github.com/shoujiki-panman/aidoku)`
- 取得済みはキャッシュから返す。**同じページを二度取りに行かない**
- HTTPの `Last-Modified` / `ETag` もcache metadataへ保存する。古いcacheは値なしとして読む
- **申請の送信は一切しない。読み取りのみ。**

取得したHTMLは `crawler/htmlutil.py` で、本文とリンクだけでなく次も構造化する。

- `<title>`
- `<meta name="description">` と `og:*`
- h1〜h6の文書順とlevel
- JSON-LD生文字列と、その中の `dateModified` / `datePublished`

これらは候補ページごとに `crawler/out/discovery_*.json` へ残す。見出しの有無を
本文全文検索で代用せず、構造として確認できるようにするため。

### ② 抽出 — `extractor/extract.py`

候補の上から「**PDFでない・200が返る・本文200字以上**」の**最初の1件**を採点対象にする（`pick_page()`）。

`.pdf .doc .docx .xls .xlsx .ppt .pptx .zip .rtf .odt .ods .csv` は選ばない。
**この除外が無かったため、台東区は `.docx` を採点されて0点になっていた**（2026-08-05 に修正・PR #48）。

| | 値 |
|---|---|
| 本文の上限 | 18,000文字（超えたら `truncated: true`） |
| 渡すリンク数 | 40件 |
| 追従リンク | `--follow` 指定時のみ、**各Test Case最大2本** |
| モデル | `claude-sonnet-5`（`claude -p` をローカルログインで呼ぶ） |

#### Evidence Check — `evidence_check.py`

AIが返した引用 `evidence` を、**そのTest CaseでAIへ本文として渡した範囲**と照合する。
範囲は本体ページと、そのTest Caseが `--follow` で開いた最大2ページで、
各ページ18,000文字まで。別のTest Caseが開いたページは混ぜない。
JSON-LDやリンク一覧は照合対象に含めない。

| verdict | 意味 |
|---|---|
| `exact` | 本文にそのまま存在する |
| `normalized` | NFKC・空白・引用記号などのゆれを吸収すると存在する |
| `partial` | 正規化後に15文字以上の連続一致があるが、引用全体は未確認 |
| `missing` | 上のどれにも当たらない。捏造とは断定せず監査フラグにする |
| `too_short` | 8文字未満で、照合しても意味がない |
| `not_applicable` | `found=false` で、引用ではないため対象外 |
| `not_checked` | 必要なキャッシュが欠け、判定できない |

`missing` でも `found=false` へ変更しない。公開スコアを裏で書き換えず、
項目の `evidence_check` と出力全体の `evidence_summary` に監査結果を残す。

2026-08-16に既存73出力へ遡って適用した結果:

- 到達71件が使った117 URLはすべてキャッシュあり。未到達は2件
- 照合対象127項目: verified 104（exact 74 + normalized 30）/
  partial 23 / missing 0 / too_short 0
- `found=false` の157項目は対象外

**これは既存73出力内の確認結果であり、別の自治体・手続き・将来の出力まで
Evidenceが正しいと一般化できるサンプルではない。**
集計は `analysis/out/evidence-check-summary.json` に残す。

#### fact_type単位のTest Case

4項目はまとめて聞かず、`service / fact_type / question / test_case_version`を持つ
独立Test Caseとして、**1回のLLM呼び出しに1 fact_typeだけ**を入れる。別の項目を
同じプロンプトへ入れたとき、追加した語が他項目の判定へ漏れた実測があるため。
`--follow`で「リンク先にあり」と返した項目だけは、同じTest Caseの2回目として再実行する。
リンク先はTest Case間で共有しない。

出力の`test_cases[]`が測定単位で、到達失敗も4件の`found: false`として残す。
各callは`test_cases[].attempts[]`へ残し、`result`は最後のattemptと同じ値にする。
PDF等へのredirectは本文を読まず、`llm_called: false`の観測attemptとして残す。
既存の`items`は`test_cases[].result`から機械的に復元し、採点・公開画面との互換性を保つ。

### ③ 書き出し — `analysis/export_dashboard.py`

`extractor/out/` の結果を、画面が読む `web/data/scores-<手続き>.json` にする。

**八王子市は既定で除外**（23区の集計に混ぜないため）。
画面に出す会話文は測定用Test Caseではなく、`targets.json`の`display_question`を使う。

### 測定条件の記録 — `measurement.py`

新しい測定は、探索時に `MAX_DEPTH` / `BEAM` / `MAX_FETCHES` と実行時刻を保存し、
抽出時に次の条件を合わせて `measurement` へ残す。

- `measurement_version` / `prompt_version`
- `follow` / `max_follow`
- `max_depth` / `beam` / `max_fetches`
- `max_text_chars` / `max_links`
- `model` / `model_version`
- 抽出の `run_at` と探索の `discovery_run_at`

`prompt_version` は抽出用とオンライン明示用のプロンプト本文から作るSHA-256で、
手動更新ではない。公開JSON生成時は実行時刻以外の条件を比較し、1つでも違えば停止する。
条件記録のある新結果と、記録開始前の結果も混ぜない。

既存73件は測定時の実行時刻などを復元できないため、推測で埋めず
`recording_status: legacy_unknown` としている。これは「条件が同じ」ではなく、
**比較できるだけの記録が無い**という意味。

---

## 3. 点数（**2つの道がある。混ぜない**）

### A. 公開ダッシュボードの点数 ← 画面に出ているのはこれ

```
4項目 × 20点（読めた/読めない の2値） ＋ オンライン明示（明記20 / 曖昧10 / 記載なし0）
= 100点
```

- **各項目は2値。だから判定が1つ変わると20点動く**
- 対象: 23区（`web/data/scores-*.json`）
- **人手の正解データを1行も使っていない**

### B. 採点器の検証（`scorer/`） ← 公開の点数とは別物

人手のゴールデンセット（`scorer/golden/tennyu.csv`・**3自治体12行のみ**／練馬・江戸川・八王子）と
突き合わせ、**部分正解**を含めて採点器そのものの精度を見る道。

- ここで測ったぶれ幅が **最大2.0点**
- **この「±2点」は 23区の点数にはかからない。**別方式・別対象

> **言い間違えやすいので固定する。**
> 「23区の点数のぶれは±2点」は**誤り。**23区のぶれ幅は測っていない。

---

## 4. 揃っていないところ（**聞かれる前に言う**）

### 4-1. 転入届は、同じ日に測っていない

出力ファイルの更新時刻から:

| 測った日 | 件数 | 中身 |
|---|---|---|
| 2026-07-21 | 3件 | 練馬・江戸川・八王子（ゴールデンセットの3つ） |
| 2026-07-22 | 20件 | 残り |
| 2026-08-05 | 1件 | 台東区（`.docx` バグの修正後に測り直し） |

**2週間にまたがっている。**児童手当と粗大ごみは 2026-08-11 の20分間に収まっている。

### 4-2. 東京都は、23区と条件が違う

23区は `--follow` あり（実際に追従が使われた区: 転入届 9/23・児童手当 10/23・粗大ごみ 10/23）。
**東京都パスポートは `--follow` なしで測った。**この1件は23区と横に並べられない。

### 4-3. 同一ホスト制限が何を落としたかは、分からない

「採点した69ページは全部トップと同じホストだった」は**同語反復。**
同一ホストしか選べないのだから当たり前で、**除外された側は見えない。**
粗大ごみで外部の申込サイト（別ホスト）を使う区がある可能性は残る。

### 4-4. ぶれ幅を測っていない

23区の点数は**各1回の判定**。同じページを2回測っていない。

### 4-5. 303文字の実験は再現できない

入力した `before.html` / `after_b.html` が残っていない（作業を一時ディレクトリで行ったため）。
**判定も各1回。**

### 4-6. モデルの記録が2件抜けている

`extract_edogawa_sodaigomi.json` と `extract_hachioji_sodaigomi.json` に `model` が無い。
他71件は `claude-sonnet-5`。

### 4-7. ビーム幅は、こちらの選択

深さ3で親1ページあたり上位3リンクしか追わない。
**東京都パスポートの本体ページは119リンク中12位で、ここに切られた。**
本物のAIエージェントがもっと辿る可能性はある。

### 4-7b. 0点の原因を切り分けた — 探索ではなく「採点したページの選び方」（2026-08-17）

上に「0点のうちどこまでが道具の制約かは、切り分けていない」と書いていた。切り分けたので書き換える。
**新規クロールはしていない。**取得済みキャッシュと `crawler/out/discovery_*.json` だけを読んだ結果。

**探索は正解ページに届いていた。届いた候補を採点していないだけだった。**

`extractor/extract.py` の `pick_page()` は、探索が集めた**25件の候補のうち条件を満たす最初の1件だけ**を
返す。残り24件はすべて取得済み（status 200）なのに、一度も読まれない。

実例（墨田区・転入届）。採点したのは候補[0]「転入届・転居届と併せて行う『電子証明書の発行』の
同一世帯員による代理手続きについて」で、リンク文字列に「転入届」が入るためスコア最高になったが、
転入届本体の説明ではない。一方**候補[7]「住民登録の届出」**（取得済み・13,316文字）の本文には

> 実際に墨田区に住み始めてから**14日以内**に転入の届出をしてください。

と書かれている。**公開データではこの区の「期限」を0点として出している。**

4項目とも読めない18組を同じ方法で調べた結果:

| 採点したページに無く、別の取得済み候補にあった語の数 | 組数 |
|---|---|
| 3語以上 | 3組（荒川区・転入／北区・児童手当／北区・粗大ごみ） |
| 2語以上 | 8組 |
| 1語以上 | 16組 |
| 0語 | 2組（千代田区・児童手当／江戸川区・粗大ごみ） |

⚠️ **これはキーワードによる目視スクリーニングであって、LLMによる判定ではない。**
言えるのは「本命ページが別にある可能性が高い」まで。確定にはその候補で抽出をやり直す必要がある。
**したがって、この18組の0点を「その区が書いていない」と読んではいけない。** 詳細は Issue #90。

### 4-7c. 「その区が書いていない」は、5つ揃って初めて言える（2026-08-30）

§4-7b と §6 は、この言い方を**禁止**している。禁止は正しい。
ただし「では何が揃えば言えるのか」がどこにも書いていなかったので、ここに置く。

`analysis/sweep.py`（虱潰し）で1項目ずつ確かめる。**5つ全部が揃った項目だけ**が対象。

| # | 条件 | 揃っていないと何が起きるか |
|---|---|---|
| ① | **候補を点数順に、残らず読んだ**（`stopped: exhausted`） | 上限で止めたものを混ぜると、ただの打ち切り |
| ② | **読んだ (URL, 項目) を全部記録した**（`visits_*.json`） | 何を見たか残らず、次に確かめられない |
| ③ | **添付（PDF/Word/Excel）も本文として読んだ** | 実測で **321ページ読んで添付0本**だった |
| ④ | **その手続きの言葉で聞いた** | 実測で、AIが手数料表を読んだ上で却下していた |
| ⑤ | **読めなかったページが1本も無い**（`errors: 0`） | 「読めなかった」が「無かった」に化ける |

③④は 2026-08-30 に**破れているのが見つかった**条件である。どちらも
「読めた／聞けたつもり」で数字が出ていた。**出た数字の形は正しかった。**

- ③ 添付をHTMLとして読もうとして文字化けし、手がかりの語に当たらず静かに落ちていた
- ④ 質問文が**転入届の説明を全手続きに使っていた**。児童手当・粗大ごみに
  「他の市区町村から引っ越してきたときに出す届出です」と教えていた。
  墨田区の粗大ごみで、AIは処理手数料一覧表を正しく読んだ上で
  「転入時の届出の手数料ではない」と却下した

間違っていた出力は消さずに `analysis/out/known-wrong/` に置いてある。
**間違いの実物が無いと、次に同じ形をした数字を見抜けない。**

⑥として「言い切らない」も添える: ①〜⑤が揃っていても言えるのは
**「候補を1本残らず読んだ上で、その項目は見つからなかった」**であって、
区のサイト全体に無いことの証明ではない。探索の届く範囲が上限である（§4-7）。

### 4-8. 既存73件の測定条件は後から完全には戻せない

測定条件の記録を始める前の出力なので、実行時刻・プロンプト版・`--follow` 指定の有無を
ファイルだけからは確定できない。現在の定数を過去データへ書き足すことはしていない。
公開JSONの `measurement.comparison_status` は `legacy_unknown` とする。

---

## 5. 再現の仕方

```bash
cd crawler   && python3 discover.py -m <自治体ID> -p <手続きID>
cd extractor && python3 extract.py  -m <自治体ID> -p <手続きID> --follow
python3 analysis/apply_evidence_check.py
cd analysis  && python3 export_dashboard.py -p <手続きID> --out ../web/data/scores-<手続きID>.json
```

`--follow` を付けるかで結果が変わる。**23区の公開値は付けて測っている。**
`apply_evidence_check.py` はキャッシュだけを読み、元の `extractor/out/*.json` は上書きしない。
新しい抽出は、測定条件を持つ探索JSONだけを受け付ける。古い探索JSONを使う場合は、
先に `crawler/discover.py` をやり直す。

テスト: `crawler/test_polite_fetch.py`（16件）・`crawler/test_discover.py`（9件）・
`extractor/test_extract.py`・`test_evidence_check.py`・
`analysis/test_apply_evidence_check.py`・`scorer/test_score.py`

---

## 6. 言わないと決めていること

- ❌ 「23区の点数のぶれは±2点」— **別方式・3自治体で測った値**
- ❌ 「源内に採用された」「源内互換」— 言えるのは**仕様に準拠**まで
- ❌ 本物のAIエージェントが来ているかのような言い方 — **門番の来訪は0件**
- ❌ 断定できるだけのサンプル数が無いのに、傾向として書くこと
- ❌ **4項目とも0点の18組を「その区が書いていない」と書くこと** — §4-7b。
  探索は本命ページに届いていて、こちらが採点していないだけの可能性が高い
  - ✅ **例外は虱潰し（`analysis/sweep.py`）を通した項目だけ** — §4-7c の5条件が
    全部揃ったものに限る。その場合も言えるのは
    **「候補を1本残らず読んだ上で見つからなかった」**まで
