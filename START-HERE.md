# START HERE — AI読（アイドク）

新しいAIセッションを始めたら、このファイルと [STATUS.md](STATUS.md) を最初に読む。
セッションを終える前に、必ず STATUS.md を今日の状態に更新する。

> ## 🚨 期日
> - **エントリー: 2026-07-27（月）17:00** ← 逃すと出場不可。文章は [ENTRY-DRAFT.md](ENTRY-DRAFT.md) にある
> - 作品提出: 8/23 17:00（プレゼン動画2分＋16:9資料が必須・事前収録は予約制）
> - Final Stage: 10/17

## 何を作っているのか（1枚で）

**自治体サイトのURLを入れると、AIがどこまで読めるかを採点し、読めない箇所と「直す文面」まで出す。**
デジタル庁OSS「源内」のAIアプリとして、職員がいつも使う場所で動く。

```
① URLを入れる     ② AIが読む        ③ 採点            ④ 処方箋
自治体の手続き  →  4項目を抜く   →  読めた/読めない →  どこに何を書けば
ページ            （必要書類・窓口   を点数に          伝わるかを、そのまま
                   ・期限・手数料）                    貼れる形で出す
```

**調査で終わらせない。** 職員が自分で気づいて、自分で直せる道具（内省）にする。

### 実測でわかっていること（2026-07-22・23区）

- 4項目すべて読み取れたのは **港区だけ**
- **5区**（世田谷・中央・台東・墨田・荒川）はほとんど読み取れない
- 手数料は **22区** で見つからない（実際は無料）

### 処方箋についての実測（2026-07-26・大事）

生成した文面を**そのまま貼っても点は上がらない**（3区で検証・全部0点上昇）。
**（　）を実際の値で埋めてから貼ると上がる**（世田谷 0→100 / 新宿 0→80）。
これはバグではなく「**AIが役所の情報を捏造しない**」ための設計。
だから「AIが直す」ではなく「**AIは穴の場所と書き方を示す。埋めるのは職員**」と言う。

## 動かす

```bash
cd <repo>/gennai_app

# 1) AI読API（判定エンジン）
AIDOKU_PORT=8791 python3 server.py &

# 2) 源内バックエンドの代わり
python3 stub_backend.py &

# 3) 源内Webフロント（本物・別途クローンが必要）
npm --prefix <gennai-web> run web:dev     # → http://localhost:5174/
#   ★ web:devw ではない。devw は setup-env.sh 経由でAWSを要求する

# 4) デモ用に23区の履歴を積む（画面右にランキングが並ぶ）
python3 seed_history.py
```

止めるときは `pkill -f "<gennai-web>/node_modules/.bin/vite"`。
**`pkill -f vite` だけだと mulmoclaude（5173）を巻き添えにする。**

## 言い方の線（審査で崩されないため）

- ✅ 「源内のAIアプリAPI仕様（2026年3月版）に**準拠**」
- ❌ 「源内に採用された」「源内互換」← 本物の源内には未登録
- ✅ データは実在の自治体公開ページのみ（自作しない）
- ✅ 判定の出どころ（実測値／その場で取得）を画面に明記する

## 進め方

- 設計を決める前に、**他の事例を先に見る**
- 作ったら**画面か数字で確認**してから次に行く
- 分かったことは [LEARNING-LOG.md](LEARNING-LOG.md) に書く
- 「〜のはず」で進めない。**確かめてから乗る**（この1週間で6回、前提が崩れた）

詳細は [STATUS.md](STATUS.md)。企画の経緯は [reports/](reports/) に全部残っている。
