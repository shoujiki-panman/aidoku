# 東京エージェント・レディネス調査 — Claude Code 引き継ぎ仕様

> 都知事杯オープンデータ・ハッカソン2026 応募作品。提出締切 **2026-08-23(日) 17:00**(時刻厳守、以降は修正も不可)。
> First Stage 審査は **8/26〜30 のプレゼン動画収録**(30分枠・早い者順予約、提出フォームから予約)。実質の勝負は「提出物 + 動画での語り」。
> 既存コンセプト「AI都民シミュレーター」の深化版。AI都民が「意見を言う」段階から「実際に手続きを試みる」段階へ進化させる。

---

## 1. プロジェクトの目的

住民のAIエージェントが行政手続きを代行する時代に、東京62区市町村の行政情報が「AIから読めるか」を実測し、**エージェント手続き完遂率**という新しい派生指標で区市町村ランキングを作る。

- 課題: 行政情報は人間が読む前提(PDF・電話受付・バラバラな構成)。AIからの可読性を測る指標が存在しない
- メッセージ: 「開放と防御はセット」(ミトス以降の文脈)。善意のエージェントを受け入れるには識別・信頼設計が必要
- ストーリー接続: デジタル庁はガバメントAI「源内」(2026年4月 GitHub/MITでOSS公開)で**職員側**のAIを整えた。本企画は次の段階、**住民側**のAIを受け入れる準備度を測る——という位置づけで提案する
- 審査戦略: Grand Prix 勝者の共通項 = ①新規の派生指標 ②締切前の実測 traction。両方を満たす

## 2. スコープ

### やること
1. **ペルソナ生成**: 国勢調査・世帯統計から統計的に代表的なAI都民ペルソナを生成(例: 江戸川区・共働き・子2人)。既存シミュレーターの千人を流用可
2. **読解タスク実行**: 各ペルソナに対応する手続きについて、区市町村の公式サイトから「必要書類・窓口/オンライン可否・期限・手数料」をエージェントに抽出させる
3. **採点**: 抽出結果を正解データと突合し、自治体×手続きごとにスコア化
4. **信頼設計チェック**: 公開情報から確認できる受け入れ態勢(robots.txt、エージェント/bot向けの意思表示、構造化データの有無等)をチェックリスト化
5. **可視化**: 公開ダッシュボード(既存モックを流用)。ランキング + 可読性×信頼設計の2軸マップ + オープンデータ整備度との相関

### 成果物のパッケージング(二枚看板)
- **調査が本体、ダッシュボードが顔**。同じデータを二つの読者向けに見せる:
  - 住民向け: 「自分の区はAIに手続きを頼めるか」
  - 行政向け: 「うちのスコアと弱点項目(どこを直せば何点上がるか)」
- このため extractor/scorer は**失敗理由を必ず構造化して記録**する(例: `手数料がPDF内のみ` / `窓口情報がリンク切れ` / `構造化データなし` / `電話でのみ確認可`)。ランキングの順位より、この改善箇所リストが行政向けの価値の中核。批判ではなく応援のトーン(「◯◯区はここを直せば+20点」)で出せるデータ構造にする
- 失敗理由の集計には**「電話送客率」ビュー**を含める: 「サイトに記載なし・電話でのみ確認可」と判定された項目の割合を自治体別に集計する。実装追加なし、`analysis/` の集計切り口として用意するだけ。プレゼンでは「サイトで完結しない情報は電話に流れ、電話対応が庁内照会をさらに生む——本調査は電話負荷の上流の蛇口を測っている」という語りに使う(スコープは広げない)

### やらないこと(安全上の制約 — 厳守)
- 実際の申請送信・フォーム送信は**しない**(読み取りのみ)
- 脆弱性の探索・診断に類する行為は**一切しない**。信頼設計チェックは「公開されている意思表示の有無」の確認に限定
- クロールは行儀よく: robots.txt 遵守、リクエスト間隔 ≥ 3秒/ドメイン、User-Agent にプロジェクト名と連絡先を明記、キャッシュ徹底(同一ページの再取得禁止)

## 3. 採点対象

3領域 × 62区市町村(23区 + 26市 + 5町 + 8村 ※島しょ含む可否は Phase 1 で判断):

| 領域 | 代表手続き | 選定理由 |
|---|---|---|
| 引越し | 転入届・転出届 | 全自治体共通・頻度最大 |
| 子育て | 児童手当の申請 | 標準化業務・世帯属性と連動 |
| ごみ | 粗大ごみ収集の申込 | オンライン化格差が大きく差が出る |

### スコアリング(案 — Phase 1 で確定)
手続きごとに以下を採点、合計を完遂率に正規化:
- 情報到達(トップページから手続きページに辿り着けたか): 20点
- 抽出正確性(必要書類・窓口・期限・手数料の正答): 40点(各10点)
  - 1項目の点は `10 × (伝わった必須要素数 ÷ 必須要素数)`。判定器に「正解/部分正解」を
    選ばせず、必須要素ひとつずつに yes/no を答えさせて機械的に点にする（2026-07-22 変更）
  - 必須要素は「手続き×項目」ごとの固定スロット表に従い、ゴールデンCSVの
    `required_elements` 列に人手で書く。サイトがその要求を課していないスロットは `-` にして
    分母から外す（EU eGovernment Benchmark の non-applicable と同じ扱い）
- 機械可読性(HTML か PDF のみか、構造化データ有無): 20点
- オンライン完結可否の明示: 20点

正解データの作り方: 自治体標準オープンデータセット準拠データ(練馬区等が公開済み)で突合できる項目はそれで採点。突合できない項目は3自治体分を人手でゴールデンセット化し、採点プロンプトの妥当性を検証してから全体に展開。

## 4. 使用オープンデータ

| 用途 | データ | 入手先 |
|---|---|---|
| ペルソナ生成 | 国勢調査(区市町村別人口・世帯構成)、転入転出 | e-Stat / 東京都オープンデータカタログ |
| 採点の答え合わせ | 自治体標準オープンデータセット準拠データ(子育て施設・ごみ等) | catalog.data.metro.tokyo.lg.jp |
| 文脈づけ | 都構造改革推進チーム「デジタル化された行政手続の満足度等調査」 | 同カタログ |
| 相関分析 | カタログの区市町村別データセット掲載数 | 同カタログ |

## 4.5 運営提供リソース(参加者特典 — 活用方針)

- **Cloudflare Paidプラン相当(Workers / Pages / R2 / D1)**: `web/` ダッシュボードのホスティングは **Cloudflare Pages に確定**。採点結果 JSON は R2 または D1 から配信。チーム単位の事前申請が必要(1人でも「1名のチーム」で申請可)→ ユーザーが申請する。Claude Code は申請完了を待たずローカルで開発し、デプロイ設定は Pages 前提で書く
- **OpenCode + Workers AI(Qwen3 30B / Qwen2.5 Coder 32B / GPT-OSS 20B)**: 日本語の抽出精度が生命線のため**抽出・採点の主役には使わない**。クレジット枠は HTML 整形・前処理など失敗してもよい下処理にのみ検討(必須ではない)
- **提出ガイドの著作権注意(重要)**: 自治体サイトの画面キャプチャを資料・ダッシュボードに使う条件が提出ガイドに規定されている。**スクショを成果物に載せる前に必ずガイドを確認**(本企画はサイトを題材にするため特に関係が深い)。地図利用時は Google のロゴ・帰属表示を隠さない・改変しない

## 5. アーキテクチャ(HermesAgent 流儀)

```
repo/
├── CLAUDE.md              # 本ファイル
├── personas/              # ペルソナ生成 (統計 → JSON)
├── crawler/               # 取得層 (Flow): polite fetch + キャッシュ
│   └── cache/             # 生HTML保存。再実行はここから (Stock)
├── extractor/             # 読解層: LLMで4項目抽出 → JSON
├── scorer/                # 採点層: 正解突合 + ルーブリック採点
├── trust_check/           # 信頼設計チェックリスト (静的確認のみ)
├── analysis/              # 相関・2軸マップ用の集計
└── web/                   # 可視化 (既存モックのHTML/Reactを拡張)
```

- リスク分類: crawler は Layer B(外部アクセス・要レート制御)、extractor/scorer は Layer A(ローカル完結)、実申請系は Layer C として**実装禁止**
- LLM 呼び出しは抽出と採点で分離し、採点プロンプトはゴールデンセットで正答率を検証してから固定する
- 採点器の改善はポケカeval方式: ゴールデンセット(人手採点 約30件、作り込まない)での正答率を唯一の指標とし、プロンプト変更ごとにヘッドレスのバッチで再計測する。感覚での変更は禁止
- 抽出エージェントには探索優先順位を明示する: HTML本文に明記 → 構造化データ → PDF内 → リンク先1階層 → 「見つからず」と正直に報告(推測での補完は禁止)
- すべての中間生成物(HTML キャッシュ、抽出 JSON、採点 JSON)を保存し、再現可能にする
- 参考実装: デジタル庁「源内」のOSS(`digital-go-jp` の genai-web / genai-ai-api)。行政実務向けAIアプリのプロンプト設計・マイクロサービス分割の参考にする。**依存はしない**(fork も組み込みもしない、読むだけ)

## 6. マイルストーン(逆算)

| 期日 | 到達点 |
|---|---|
| 7/26 | Phase 1: パイロット3自治体(例: 練馬・江戸川・八王子)で全パイプライン疎通、採点基準確定 |
| 8/02 | Phase 2: 23区完走、ランキング v0 |
| 8/06-09 | キックオフウィーク(8/6 イベント、8/9 テーマWS&チームビルディング)。**8/9 がメンバー募集の本命の場** — 動くパイロット + ランキング v0 を持って参加する |
| 8/09 | Phase 3: 62自治体完走、2軸マップ + 相関分析 |
| 8/12 | LinkedIn/note でランキング先行公開(traction 獲得開始) |
| 8/20 | 応募資料・デモ最終化。**提出はこの日に済ませる**(締切ギリギリにしない)。提出フォームから動画収録枠(8/26-30、早い者順)も同時予約 |
| 8/23 | 提出締切 17:00(バッファ) |

**縮小ルール(重要)**: 8/02 時点で23区が完走していなければ、62自治体は捨てて **23区のみに確定**する。派生指標の新規性は23区でも成立する。数字の存在が装飾より常に優先。

**優先順位の原則**: 判定基準は一つ——「8/23 に実測された数字が存在するか」。
1. 必須: パイロット3自治体でクロール→抽出→採点が一周すること(最優先、これ以外は保留可)
2. 安い割に効く: ゴールデンセット検証(30件で十分)、robots.txt 等の信頼設計チェック(数時間で済む範囲)
3. 装飾(実装に影響させない): 源内・ミトスへの言及は応募資料の文章のみ
4. 保留: メンバー募集はパイロットが動いてから。可視化の作り込みは Phase 2 以降

## 6.5 発信計画(traction 獲得)

- **本線は実名 LinkedIn / note**(行政DX読者層がすでにいる。チッカイギ連載・調達記事の続編として出す)。応募書類・省庁応募のポートフォリオに使うため、本人名義であることが必須
- X(実名側)は要約と拡散用。正直パンマン名義は**使わない**(文脈が異なる)
- プロジェクト専用アカウントは作るとしても「ダッシュボードの公式窓口」のみ。発信の主戦場にはしない
- 8/12 の先行公開記事の構成案: ①住民視点のフック(「あなたの区はAIに手続きを頼めるか」) ②ランキングとダッシュボードへのリンク ③行政視点の改善箇所リスト(応援トーン) ④源内→住民側AIというストーリー接続
- Claude Code の担当: 記事に貼るランキング表・図版(2軸マップ、正誤表)を `analysis/` から画像/表として出力できるようにしておく。記事本文はユーザーが書く

## 7. 最初のタスク(Claude Code への依頼 — このスプリントだけに集中)

1. `crawler/` の polite fetcher を実装(robots.txt パーサ、3秒間隔、UA明記、ディスクキャッシュ)
2. パイロット3自治体(練馬・江戸川・八王子)の「転入届」ページを取得し、`extractor/` で4項目(必要書類・窓口/オンライン可否・期限・手数料)を抽出
3. 人手ゴールデンセット(ユーザーが用意する CSV: 自治体×項目×正解)と突合するスクリプトを `scorer/` に用意
4. 結果を1枚の Markdown レポートに出力(自治体×項目の正誤表 + 採点器の正答率)

**このスプリントの完了条件**: 「練馬区の転入届について、エージェントの抽出結果と正解の突合表が1枚出ること」。ペルソナ生成・可視化・62自治体展開・信頼設計チェックは、これが出るまで着手しない。`web/` に触る場合もローカル開発のみ(デプロイは Cloudflare Pages 前提の設定だけ用意)。

**ユーザー側タスク(Claude Code の担当外)**: ①ゴールデンセット CSV の用意 ②Cloudflare 特典のマニュアル精読と申請 ③提出ガイドの著作権条件の確認 ④キックオフウィーク(8/9)の申込

## 8. 判断に迷ったら

- 「実申請に近づく操作か?」→ Yes なら実装しない
- 「このアクセスは相手サーバーに優しいか?」→ No なら間隔を空ける・キャッシュを使う
- 採点基準の変更は必ずゴールデンセットで再検証してから全体に適用
