# CLAUDE.md — AI読（アイドク）

自治体サイトを、住民のAIエージェントが読めるかどうか実測して直す道具。
**この作品の前提は「住民のAIエージェントが、住民の代わりに行政サイトへ来る」**。
「住民がAIに質問する」ではない。対外文（提出・LT・デッキ・動画）は必ずエージェント来訪の側で書く。

最初に読む → [START-HERE.md](START-HERE.md) ／ 進捗と期日 → [STATUS.md](STATUS.md)

## コマンド

```bash
# 門番のテスト（外部ネットワーク不要）
cd gatekeeper && node test_local.mjs && node test_worker.mjs && node test_nlweb.mjs && node test_mcp.mjs

# 採点のぶれを測る（5回×3自治体・約8分）
python3 scorer/stability.py -p tennyu --runs 5 --tag <識別子>

# 作品（源内AIアプリとして）を動かす手順は START-HERE.md の「動かす」
```

## 変更したら（MUST）

1. **`change-check` を回す。** 一次情報／影響範囲／前提の3行をチャットに書く。
   飛ばすと古い盤面のまま先へ進む（実際に STATUS.md が3日放置された）
2. **テストを回す。** 現在 78 PASS / 0 FAIL
3. **影響を受けた文書を同じ変更の中で直す。** 特に STATUS.md と README.md

## 進め方（依頼を受けてから）

1. **依頼がまとまったら GitHub Issue にサマリを投稿する。** チャットの中だけで進めない
2. **`plans/` に計画をファイルとして置いてから実装を始める。** MUST。
   計画がリポジトリに残らないと、何をやったか後から追えない
3. 実装する
4. **PRを出す。PRコメントに、元の依頼を内容を増やさずに清書して残す。** MUST。
   依頼が複数あるときは bullet list にする

チャットで出た重要な情報は、コミットメッセージかIssueに残す。チャットは消える。

## PR

**1 PR = 1目的。MUST。** 複数の目的が混ざったら分割する。1行のPRになってよい。

**理由: 書くより読む方が苦労する。** 数が多くても1つ1つが簡単なら理解しやすい。

- 混ざったまま出さない。順番に依存があるときは前のPRをbaseにして積む
- 3本以上に割るときは傘Issueで束ね、各PRに `Part N of M splitting #XX` と書く
- 実例: PR #16（9コミット・5目的）→ #18〜#25 に分割、傘 #17
- **CIが通ったらマージする**
- **マージしたら振り返る。** 途中で指示ややり直しがあったら、それを
  この CLAUDE.md に足すべきか毎回検討する。**同じ注意を2回言わせない**

## テスト（MUST）

- **lint / unit test / formatter は必須。使い捨てのツールでも例外にしない**
- **CIでもテストを回す**
- **unit test を書くコストはもう0。** edge case まで網羅的に書く

## Lint と重複（MUST）

**AIにコードを書かせる以上、規約は人のレビューではなく機械で守る。**

```bash
ruff check .        # 設定は pyproject.toml（E,F,I,B,UP,C4 / 120桁）
npx jscpd .         # 設定は .jscpd.json（ソースのみ・重複5%まで）
```

- **CIで落とす。** 報告だけして誰も直さない設定にしない
- **ルールを足すときは、既存コードも同じ変更で直して緑のまま増やす**
- 作品本体は Python 標準ライブラリのみで動く方針は変えない。ruff / jscpd は開発時だけ

**まだ入れていないもの**: 型チェック・Mutation Testing・AI同士のCross Review。提出後に入れる。

## テストが通っても仕様は保証されない（MUST）

**外の世界について何かを断言する処理には、その前提が成り立っているかを検査する段を持たせる。**

#86 が実例。「この項目がページに書かれていない」と区へ通知していたが、そもそも
**対象の手続きのページを見ているかを誰も検査していなかった**。テスト27件は全部通っていたし、
Lintも通る。テストは「コードが仕様どおりか」しか見ないので、仕様が外れているときは無力。
気づいたのは人が画面を見たときだった。**出力の実物を人が見る段を、CIと同格で置く。**

## このファイルの書き方

- **MUST / NEVER / DO NOT** = 絶対にやる・やらない。守れないなら書かない
- **SHOULD / CONSIDER** = 優先度が下がる。絶対でないものはこちらを使う
- 強度を正しく付けないと、全部が「なんとなくの心がけ」に落ちて効かなくなる

## 日付（MUST）

**今日の日付が要るときは、必ず `date` コマンドで取得する。**
モデルの内部知識に頼らない。日付は文書・レポート名・期日の判断に直接効く。

## 数字と対外文（MUST）

- **数字は一次情報で裏を取ってから出す。** 手元メモや過去の自分の文章は一次情報ではない
  （READMEに「Cloudflareのエッジで2026年3月から本番稼働」と出典なしで書いていた。実際は2025年7月）
- **裏が取れないものは「確認できていない」と書く。** 推測で埋めない
- **NEVER: 見本を本物として見せる。** 画面と JSON に `is_sample` を出し、
  最上段に「本物のAIエージェントの来訪 0件」を出す
- **NEVER: 「源内に採用された」「源内互換」と書く。** 言えるのは「AIアプリ仕様に準拠」まで。
  ローカルで起動した源内で確認したなら「ローカルで起動した源内で」と書く
- 採点はLLM判定なのでぶれる。**最大2点は3自治体12行の採点器検証だけ。**
  公開23区は各1回で、ぶれ幅を測っていない。2つを混ぜない

## 記録しないもの（NEVER）

- **人（ブラウザ）は記録しない。** 門番が記録するのは署名つきエージェントだけ
- 署名が示すのは「どのAIサービスか」であって、その先の利用者ではない

## 触るときの罠

- **`pkill -f vite` を使わない。** 同じマシンの別のViteアプリを巻き添えにする。パスで絞る
- 源内の起動は `web:dev`。**`web:devw`（末尾w）はAWSを要求する**
- `server.py` のポートは環境変数 `AIDOKU_PORT`。`--port` 引数は**無い**（黙って無視される）
- **ポート 8787・8791・8177 は別プロセスが居座っている。** 空きは
  `lsof -nP -iTCP -sTCP:LISTEN` で先に確認する
- 門番のランタイム検証は `--local-protocol https` が必須（鍵配布をhttpsでしか信用しない）
- テスト用の鍵を作り直したら wrangler dev を再起動する（**JWKSを1時間キャッシュする**）
- 源内のフロントは `createdDate` を**エポックミリ秒**で扱う。ISO文字列だと `Invalid time value`

## クロールの作法（変更しない）

robots.txt 遵守・3秒以上の間隔・取得結果を保存して再利用。外に触るのは `crawler/` だけ。
生HTMLは `.gitignore` で除外（自治体サイトの著作物のため）。

## 作業の進め方

- **フェーズとゴールを細かく区切る。** 1タスクは「完了条件が1行で書ける」大きさまで割る
- **1返答1論点。** 判断してほしいことが2つ以上あるなら、先に1つ出して答えを待つ
- 聞かれていない分析を足さない。市場調査や将来構想は頼まれてから
