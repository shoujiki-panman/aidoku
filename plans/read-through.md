# 読み切る — 「書いていない」と「読み落とした」を分ける

Status: 計画（未実装）。**着手は本人OK後**
関連: [`plans/reader-tools.md`](reader-tools.md)（道具の話。こちらは**渡す範囲**の話） /
[`plans/measurement-harness.md`](measurement-harness.md) / [`METHOD.md`](../METHOD.md) §6

---

## 問題

AI読は「23区中22区が転入届の手数料を書いていない」と言ってきた。
だがそれは **うちの読み取り器が「記載なし」と言った** だけである。

2026-08-25 に実データを数えたら、こうだった（転入届・24自治体）:

| | 本数 |
|---|---|
| 探索が見つけた候補URL | **592** |
| うち取得済み（本文200字超） | 558 |
| **読み取り器に渡したページ** | **41**（7%） |
| **渡さなかったうち、リンク題に「転入」等が入る本命ページ** | **305** |

ほぼ全区で **1本しか開いていない**。同じ区に、題に「転入届」と書いてあるページが
平均13本あって、そこには行っていない。実物:

- 目黒区「転入届：本人または新しく同じ世帯になるかたが手続きをするとき」（点39）**未読**
- 練馬区「転入届（国外から練馬区に引越したとき）」（点42）**未読**
- 江東区「国外への転出届・国外からの転入届」（点36）**未読**
- 葛飾区「転入届」（点24）**未読**

**だから「書いていない」は、いま言えない。**
書いていないのか、305本の方に書いてあったのかが分かれていない。

さらに悪いことに、**「なぜ開かなかったか」はどこにも記録がない。**
`crawler/out/discovery_*.json` は候補を点数つきで残しているが、
抽出側が何を選び、何を捨て、なぜ捨てたかは残らない。
記録が無いので、**読み切ったと主張することが原理的にできない。**

### 影響範囲（この計画が正しければ、下は全部やり直し）

- `web/data/scores-*.json` の「読めた／読めない」判定
- `experiment/out/fee-ask_search.json` の「22区が書いていない」（2026-08-25）
- デッキ・台本の「手数料は22区で見つかりません」
- `plans/reader-tools.md` の「リンク先にある 30セル」という見積もり

---

## 読むべきは305本ではなく90本

キャッシュ済み297本（773,119字）を機械的に見た内訳:

| | 本数 |
|---|---|
| 本命の未読ページ | 305 |
| うち PDF / Word / Excel（読む道具が無い） | **5** |
| うち本文が手元に無い（要取得） | 3 |
| うち本文がキャッシュ済み | 297（773,119字） |
| **うち手数料の語（手数料/費用/無料/かかりません）を含む** | **90** |

**手数料1項目なら、読ませるのは90本で足りる。** 平均3.75本/自治体。

品川区・豊島区は **0本**。この2区は「書いていない」が先に確定に近づく。
港区は11本（唯一「読めた」区。上がりようはないが、根拠が増えるかの検算に使える）。

### 全部読ませてはいけない（実測で確認済み）

語だけで絞ると **24/24自治体が当たった**。中身を読むと外れだった:

- 荒川区「住民票の写し 1通につき300円」← **住民票の手数料**
- 中央区「戸籍謄本 1通：450円」← **戸籍の手数料**
- 板橋区「Adobe Reader をダウンロード（無料）」
- 足立区「プラネタリウム 入場無料」

**転入届の手数料ではない。** 表読みのときと同じ失敗（見積もり12セル→実際4セル、
`plans/decisions/table-reading.md`）。**手続きで絞ってから読む。**

---

## スコープ

| # | やること | 受け入れ条件 |
|---|---|---|
| ① | **探索台帳** — 候補1本ごとに「渡した／渡さなかった・理由・点数」を残す | 転入届24自治体の候補**592本すべてに理由がつく**。理由が空欄のものが**0件** |
| ② | **読み切る** — 本命かつ手数料語を含む90本を読解に渡す | 「22区が書いていない」が**何区に変わるか**が出る。変わった区は**URL＋原文の引用位置つき** |
| ③ | **非HTMLを読む** — PDF / Word / Excel 5本 | 5本すべてに「読めた／読めない（なぜ）」がつく。**読めないものも記録する** |

**①が先。** ②③で点が動いても、台帳が無ければ「読み切った上で無い」と言えない。

---

## 測定条件に署名する（MUST）

`measurement.py` の `CONDITION_KEYS` に足す:

| キー | 値の例 | なぜ要る |
|---|---|---|
| `read_breadth` | `"agent_pick_2"` → `"strong_all"` | 読ませる範囲。**ここが変わると点が動く** |
| `non_html_reading` | `null` → `"pdf_text"` | ③の有無 |

**足さないと、道具を良くして上がった点が「サイトが直った」に見える。**
`link_order` / `table_reading` で2回やったのと同じ扱いにする。
公開済み `scores-*.json` には旧条件として `null` を追記する
（追記しないと `measurement_signature()` が KeyError で落ちる）。

条件が違う記録どうしは、これまでどおり **比較を拒否する**（`attribution()` が unknown を返す）。

---

## やらないこと（と理由）

- **592本ぜんぶ読ませない。** 手続きで絞らないと他手続きの手数料を拾う（上の実測）。
  範囲を広げるのは、90本の結果を見てから
- **判定基準を変えない。** 4項目の必須要素は人が決めたまま。
  この計画は **渡す範囲**だけを変える。基準を一緒に動かすと、何が効いたか分からなくなる
- **再クロールしない。** 297本は既に手元にある（`crawler/cache` 270MB）。
  区のサーバーに同じものを取りに行かない
- **本文を再配布しない。** `quote_withheld` を維持。読み取って判定するだけで、原文は持たない
- **他の手続き（児童手当・粗大ごみ）は後。** まず転入届・手数料1項目で通し方を確定する
- **公開サイトはまだ触らない。** 数字が動くと分かってから直す。中途半端に混ぜない

---

## 順番と見積もり

1. **① 台帳**（半日・LLM 0回）— 候補と抽出結果を突き合わせて理由を埋める。
   `analysis/check_unread.py`（2026-08-25 に測定用として作成済み）を土台にする
2. **② 読み切る**（LLM 24〜90回）— 自治体ごとにまとめて渡せば24回。
   `evidence_check.py` の根拠照合を**必須**にする。90本読ませると偽陽性が増えるため
3. **③ 非HTML**（1時間・5本）— 標準ライブラリのみの方針があるので、
   PDFテキスト抽出の手段を先に決める（外部依存を足すなら `plans/decisions/` に理由を残す）
4. 結果を `web/data` と対外文（デッキ・台本・fee_ask）に反映

### 想定される結末（どちらでも前進）

| 結果 | 意味 |
|---|---|
| 手数料が読めた区が**増える** | **AI読の数字は過大だった。** 直して出し直す。「測る側の失敗が混ざる」の3例目 |
| **変わらない** | **22区は本当に書いていない。** 305本読んだ上での結論になり、いまより遥かに強い |

---

## Progress

<!-- 追記のみ。1ステップ1行、失敗も書く。書き直さない。 -->

- 2026-08-25 実データを数えた。転入届は候補592本・取得済558本に対し、読解に渡したのは41本（7%）。本命の未読305本。`analysis/check_unread.py`
- 2026-08-25 語だけの絞り込みは24/24自治体が当たり、中身は住民票300円・戸籍450円・Adobe無料だった。手続きで絞らないと使えない。
- 2026-08-25 手続きで絞った内訳を確定: 本命未読305（PDF/Word 5・未取得3・キャッシュ済297＝773,119字）、うち手数料語を含むのは90本。品川区と豊島区は0本。
- 2026-08-25 計画のみ。実装は未着手（本人OK待ち）。
- 2026-08-26 OK。Issue #148 を投稿。ブランチ `feat/148-read-ledger`（未push・未マージの `3ba6d8a` 表読みの上に積む）。
- 2026-08-26 ① 探索台帳 実装。`analysis/read_ledger.py`。転入届599本すべてに理由がついた（空欄0）。read 41 / shown_not_chosen 237 / never_shown **290** / non_html 10 / unfetchable 21。
- 2026-08-26 台帳が2箇所で嘘をついていたので直した。(1)読んだ41本が台帳では34本だった——AIが要求して開いたリンク先は探索の候補に無いことがある。`with_missing_reads` で補った。(2)`shown_not_chosen` の理由を「AIが開かなかった」と書いていたが、AIは `MAX_FOLLOW=2` 本しか要求できない。こちらの上限がAIの落ち度に見えるので文言に上限を入れた。両方テストで固定。
- 2026-08-26 テスト22件追加（全体139 PASS / 0 FAIL）。ruff 通過。jscpd 重複0.93%（上限5%）。
- 2026-08-26 ②③は未着手。`read_breadth` / `non_html_reading` の CONDITION_KEYS 追加も未着手（②で点が動く前に入れる）。
- 2026-08-26 測定条件の署名を追加。`read_breadth`（`agent_pick` / `strong_all`）と `non_html_reading`（`none` / …）。本数は `max_follow` が持っているので `read_breadth` には書かない（2箇所に書くとずれる）。
- 2026-08-26 穴埋めを汎用にした（`analysis/backfill_condition_keys.py`）。同じ手作業を3回やっている（`link_order` / `table_reading` / 今回）。`--check` は不足があれば非0で終わるのでCIに置ける。
- 2026-08-26 最初これを再帰探索で書いて、自治体ごとの `runs`（`model` しか持たない）まで measurement ブロックとして拾った。**形を決め打ちしないことが常に安全とは限らない。** scores の `measurement` を直接見る形に直した。
- 2026-08-26 `index.json` は条件の**値**ではなく**キー名の一覧**を持っていた。穴埋めではなく `export_data_index.py` で書き出し直すのが正しい。1つの道具で両方を報告するようにした。
- 2026-08-26 `test_measurement.py` の「全条件キーに差分ケースを要求する」テストが、追加漏れをその場で捕まえた。番人として機能している。
- 2026-08-26 土台（#150）に ruff の指摘が3件残っていた（`l` の変数名2件・import順1件）。#150 側で直した。CIが赤いままだとマージできない。
- 2026-08-26 154 PASS / 0 FAIL、ruff 緑、jscpd 0.92%。②の読解本体は未着手。
- 2026-08-27 ② 実装・実行。`analysis/reread_fee.py`。未読の本命ページのうち手数料の語を含む90本を1本ずつ読解。**読み落とし0件。引用の捏造0件。**
- 2026-08-27 90本だけでは「読み切った」と言えないので、残りも潰した。`円`はあるが手数料語が無い4本＋`無償`等がある10本を手で確認（過料5万円・コンビニ10円・区の広告料5000円・切手代・Adobe無償×10）。**1つも転入届の手数料ではない。** 残り198本は手数料を表す語も金額も一切無く、書きようがない。
- 2026-08-27 結論: **「23区中22区が転入届の手数料を書いていない」は、本命305本を読み切った上での結論になった。** 計画に書いた2つの結末のうち強い方。`experiment/out/fee-ask_search.json` の前提が固まった。
- 2026-08-27 除外語の効きを実測で確認。`不要です|納付|ご負担` を入れると40本当たるが、中身は「来庁は不要です」「公金納付場所」ばかりで手数料ではない。**語を広げれば拾えるわけではない。**
- 2026-08-27 ③の準備で `crawler/officedoc.py`（Word/Excel/PDF読み・標準ライブラリのみ）を書いた。10本すべて失敗。
- 2026-08-27 原因はPDF側ではなくキャッシュだった。**`crawler/polite_fetch.py` が全応答を `write_text` で保存しており、非HTMLのバイト列が decode→encode の往復で壊れる**（29KBのWordが51,927バイトに膨張＝置換文字だらけ）。非HTMLを一度も読んでいなかったので誰も気づかなかった。③は取り直しが要る。
- 2026-08-27 キャッシュのバグを修正。`is_text_type()` で Content-Type を見て、テキストは従来どおり文字で、PDF/Word はバイト列で保存する。`FetchResult.body_bytes()` を追加し、`body()` は非テキストに空を返すようにした（化けた文字列を本文として読ませない）。テスト10件。
- 2026-08-27 壊れていた10本ぶんのキャッシュ（本文+meta の20ファイル）を消して取り直した。
- 2026-08-27 ③ 実行。**10本すべてに「読めた／読めない（なぜ）」がついた。理由が空のものは0。** Word 3本は読めた、PDF 7本は読めない（CIDフォント6・画像PDF1）。
- 2026-08-27 **「読めたふりをしない」ための関門が、実物では効いていなかった。** 「英数字か日本語が2割」で書いたため、`ja-JP en-US` の繰り返し（68,038字）と文字化け（111,553字）が「読めた」で抜けた。どちらも `isalnum()` が真になる。**仮名を数える**形に直した。関門は実物に当てるまで効いているか分からない。
- 2026-08-27 PDFライブラリは入れない決定。10本すべて様式で手数料は載らず、読めた3本にも必要書類の一覧は無い。住民のAIも同じPDFを読めないので、こちらだけ読めるようにすると住民の体験から離れる。取り消す条件つきで `plans/decisions/non-html-reading.md` に記録。
- 2026-08-27 153 PASS（crawler）/ 165 PASS（全体）/ ruff 緑。**計画の①②③はすべて受け入れ条件を満たした。** 残りは他3項目（必要書類・窓口・期限）への拡張。
