# 条件を記録した状態で測り直す

Status: 実行中（転入届）
関連: [`plans/never-looked.md`](never-looked.md)（この計画を生んだ発見） / [`plans/read-through.md`](read-through.md) / Issue #148

---

> **⚠ 下の「再現しない」は言い過ぎだった（2026-08-28 訂正）。**
> 転入届を測り直したら **96セル中93セルが一致**した。再現しないのは**児童手当**。
> 正しくは **「手続きによって再現性が違う。そしてどちらか分からないまま公開していた」**。
> 元の記述は消さずに残す。経緯は末尾の Progress に。

## なぜ要るか

2026-08-28、公開データが**再現しないこと**が確定した。

児童手当の7区（公開データでは全項目「記載なし」）に、**旧プロンプトのまま**同じ入力を与えた:

| | 記載なし | リンク提案 |
|---|---|---|
| **公開データ**（7区×4項目=28件） | **28/28** | **0件** |
| 2026-08-28 の旧プロンプト | **14/28** | 16件 |

同じプロンプト・同じページで半分になった。
公開データの `measurement` は `legacy_unknown` で、**`model_version` すら `null`**。

> **どのAIが出した数字か、記録が無い。**

`measurement_signature` を作った動機そのものが、公開済みデータには適用されていない。
**再測定しないかぎり、公開データのどの数字も「その区が書いていない」とは読めない。**

---

## この再測定で何を固定するか

`CONDITION_KEYS` 15項目すべて。特に:

| キー | 値 |
|---|---|
| `model_version` | 実際に呼んだモデル |
| `prompt_version` | プロンプト2本の sha256（#159 の修正を含む） |
| `read_breadth` | `agent_pick` |
| `non_html_reading` | `none` |
| `link_order` / `table_reading` | `score_desc` / `heading_value` |

**条件が違う記録どうしは比較を拒否する**（`attribution()` が unknown を返す）ので、
旧データと新データは自動的に別物として扱われる。**混ざらない。**

## 探索はキャッシュの再生である（重要）

`crawler/discover.py` を回し直したが、**713件中615件がキャッシュから返った**（4分50秒）。
つまり **ページの中身は当時のスナップショットのまま**。

これは狙いどおり:

- **区のサーバーに同じものを取りに行かない**（行儀）
- **変数がモデルとプロンプトだけに絞れる。** ページが変わっていたら何が効いたか分からない

**言えること**: 同じページに対して、条件を記録したうえで測るとどうなるか
**言えないこと**: 今日の区のサイトがどうなっているか（それは別の測定）

## 対象は24自治体（探索は28ある）

`crawler/targets.json` には町田・三鷹・武蔵野・東京都も入っているが、
**抽出は一度もされていない**。公開データと比べられるよう **24自治体に揃える。**
広げるのは比較が済んでから。

## 6区ずつ4回に分ける

`extractor/batch.py` の `build_batch` は**全件を作ってから書く**（途中失敗で既存出力を
壊さないための設計）。正しいが、24区一括だと **落ちた瞬間に全部消える。**

利用上限で71ページを失った実績があるので、**6区ずつ4バッチ**に切る。
1回落ちても失うのは6区ぶん。

```
1  adachi arakawa bunkyo chiyoda chuo edogawa
2  hachioji itabashi katsushika kita koto meguro
3  minato nakano nerima ota setagaya shibuya
4  shinagawa shinjuku suginami sumida taito toshima
```

旧結果は `analysis/out/remeasure/` に退避してある（比較用）。

## やらないこと

- **公開データ（`web/data`）をこの段階で書き換えない。** 差分を見てから決める
- **判定基準を動かさない。** 必須要素は人が決めたまま
- 児童手当・粗大ごみは後。まず転入届で通し方を確定する
- **4自治体を足さない。** 比較が済むまで母数を変えない

## 見るもの

| 見るもの | なぜ |
|---|---|
| 旧と新で判定が変わったセル数 | **どれだけ再現しないか**の実測値 |
| 追従した本数 | #159 のプロンプト修正が効いたか |
| 中央区・墨田区・世田谷区 | #155 の追試で「書いてある」と分かっている区 |
| 港区 | 唯一の満点区。下がったら道具側の疑い |

---

## Progress

<!-- 追記のみ。1ステップ1行、失敗も書く。書き直さない。 -->

- 2026-08-28 `crawler/discover.py -p tennyu` を全件回し直した。`recording_status: recorded` になった。713件中615件がキャッシュ、所要4分50秒。
- 2026-08-28 探索は28自治体ぶんあるが、抽出があるのは24。比較のため24に揃える（町田・三鷹・武蔵野・東京都は対象外）。
- 2026-08-28 旧結果を `analysis/out/remeasure/` に退避。バッチ1（6区）を実行開始。
- 2026-08-28 バッチ1〜4完了（24自治体）。**96セル中93セルが一致した。** 変わったのは3セルだけ。
- 2026-08-28 変化: 中央区 窓口 ✗→✓ / 世田谷区 窓口 ✗→✓（追従0→2本）/ 新宿区 窓口 ✓→✗。**逆方向のぶれも1件ある。** 道具を良くしても下がるセルはある。
- 2026-08-28 追従の合計 **17本 → 34本**（2倍）。#159 のプロンプト修正が効いた。
- 2026-08-28 中央区・世田谷区の窓口は、**#155 の追試が「書いてある」と特定していた項目そのもの**。追試と本測定という別々の道具が、独立に同じ結論に到達した。
- 2026-08-28 **訂正**: 「公開データが再現しない」は手続きによる。転入届は93/96で安定、児童手当は28件中14件がずれる。**どちらか分からないまま公開していた**が正しい言い方。
- 2026-08-28 手数料22区は変わらず。#153 の305本読み切りと合わせ、いまいちばん堅い数字になった。
- 2026-08-28 児童手当・粗大ごみの再測定は未着手。
- 2026-08-28 児童手当の探索を回し直した（28/28 recorded・5分16秒）。抽出は6区ずつ4バッチ。
- 2026-08-28 バッチ4が利用上限で落ちた（`claude -p` rc=1）。**6区ずつに切っていたので失ったのは6区ぶんだけ。** 落ちた分だけ回し直した。
- 2026-08-28 **児童手当 24区完了。判定が変わったのは 12/96 セル（12%）。** 転入届は 3/96（3%）。**手続きによって再現性が4倍違う。**
- 2026-08-28 変化: 千代田 0→2 / 北 0→3 / 新宿 0→3 / 墨田 0→1 / **中野 3→0**。上がったのが9セル、下がったのが3セル。追従の合計 14→25本。
- 2026-08-28 上がった4区のうち千代田・北・新宿は、`analysis/never_looked.py` が「全項目0点かつ追従0本」と検出した7区に含まれる。**追試・検出・本測定の3つが同じ区を指した。**
- 2026-08-28 **中野区は3項目とも落ちた。** 道具を良くしても下がる区はある。隠さず記録する。
- 2026-08-28 結論の言い直し: 「公開データが再現しない」でも「転入届は再現した」でもない。**同じ道具・同じページでも手続きによって再現性が4倍違い、公開データにはどちらだったかの記録が無い。**
- 2026-08-28 粗大ごみは未着手。
- 2026-08-29 粗大ごみの探索を回し直した（28/28 recorded・3分40秒）。抽出は4バッチ。
- 2026-08-29 バッチ3が2回落ちた。1回目はAI返答の末尾カンマ、**2回目は #159 で自分が入れたプロンプト規則が追従後の「もう追わない」と衝突した回帰**。転入届と児童手当では出ず、粗大ごみで初めて出た。**手続きを変えないと見つからない欠陥がある。**
- 2026-08-29 バッチ4は利用上限で落ちた。回し直して完了。
- 2026-08-29 **粗大ごみ 24区完了。変化 29/96（30%）。上昇17・下降2。追従 15→42本（2.8倍）。**
- 2026-08-29 **3手続きそろった。再現性は手続きによって10倍違う: 転入届3% / 児童手当12% / 粗大ごみ30%。**
- 2026-08-29 粗大ごみで最も動いたのは `必要書類`（15区で反転）。処理券の買い方が別ページにあることが多く、追従が2.8倍になって届くようになった、という筋が通る。江戸川区・八王子市は4項目とも変化。
- 2026-08-29 下降は3手続き合わせて6セルだけ。**道具の改善はほぼ一方向に効いている。**
- 2026-08-29 結論（4回目の言い直し・これが最後）: **同じ道具・同じページでも再現性は手続きによって10倍違い、公開データにはどれだったかの記録が無い。**
- 2026-08-30 **訂正: 粗大ごみの変化は 29/96 ではなく 21/96（22%）。上昇18・下降3。** PRを書く前に数え直して気づいた。元の数字は**それ自体が合っていなかった**（上昇17＋下降2＝19で、29にならない）。転入届3/96・児童手当12/96は数え直しても一致したので、誤りは粗大ごみの集計1回だけ。数え方は「4項目の `found` が旧→新で反転したセル」で3手続きとも同じ。
- 2026-08-30 **訂正: 「再現性は10倍違う」は言い過ぎ。正しくは約7倍**（転入届3.1% / 児童手当12.5% / 粗大ごみ21.9%）。**結論の向きは変わらないが、倍率を大きく言っていた。**
- 2026-08-30 訂正: 最も動いた `必要書類` は15区ではなく **14区**。`期限` は1区も動いていない。
- 2026-08-30 **訂正: 「江戸川区・八王子市は4項目とも変化」は誤り。この2区は旧・新どちらも起点ページに到達できておらず（`reached: false`）、変化は0。** 到達できなかった区を「変化した」と数えていた。`analysis/sweep.py` の `reached()` と同じ取り違えを、集計側でもやっていた。
- 2026-08-30 数え直しは PR を書くために手で計算し直しただけで、**専用の比較スクリプトが無い**のが原因。数字を出すたびに書き捨てているので、また間違える。比較は道具にすべき（未着手）。
- 2026-09-02 **72組すべてを同じ条件で測り直し完了。公開データ3本を出し直した。** `jidouteate` / `sodaigomi` は `model_version` が `null` だった＝どのAIが出した数字か誰も検算できなかった。今回が初めて条件つきの公開。転入届 65.2→63.9 / 児童手当 45.7→51.3 / 粗大ごみ 39.6→53.9。
- 2026-09-02 残り判定が `prompt_version` だけを見ていて誤答した。OCR を足してもプロンプトは変わらないので「揃っている」と言い、`export_dashboard.py` が拒んだ。**条件はひとまとまりで効く。1つだけ見て揃ったと言ってはいけない。** `tools/stale.py` を作った。残り 24組と誤答 → 正しくは67組。
- 2026-09-02 利用上限に当たると `claude -p failed (rc=1)` が延々出て、残り61組ぶん空振りしていた。`tools/run_pipeline.sh` に3連続失敗で止まる規則を入れた。**失敗を積み上げても意味がない。**
- 2026-09-02 **読める道具を増やしたのに下がった区が3つ出た**（中野区 児童手当 80→20 / 大田区 転入届 80→60 / 世田谷区 転入届 40→20）。条件差なのか揺れなのかを**区別できない**。1組を1回しか測っていないため、揺れの幅を知らない。→ `analysis/repeat.py` を作った（METHOD §4-4）。
- 2026-09-02 `analysis/repeat.py` の最初の実行が9回とも即失敗した。**zsh は `$pair` を単語分割しない**ので `set -- $pair` で `$1` に "nakano jidouteate" が丸ごと入り、`-p ""` で呼んでいた。ログの `=== nakano jidouteate  ===` の空白2つが唯一の手がかりだった。
- 2026-09-02 `extractor/extract.py` に `--out-dir` を足した。足さないと揺れの測定が**公開中の結果を上書きする**。
- 2026-09-02 **揺れを測った。下がった3組を3回ずつ。2つは揺れ、1つは違った。** 大田区 転入届＝`オンライン明示` が 記載なし/明記/明記 と割れた（同じページ・同じ条件）。世田谷区 転入届＝`窓口/オンライン可否` が 読めた/読めた/読めない。**1回の測定で20点動く。** 中野区 児童手当は3回とも同じで、揺れではなく**起点ページが旧測定と変わっていた**（hops 2→3）。
- 2026-09-02 **事実4項目は安定し、判断は揺れる。** 6組中5組で事実4項目は3回とも一致。割れたのは `オンライン明示`（あり/なしの判断）と `窓口/オンライン可否`。「書いてあるか」は安定、「はっきり書いてあるか」は揺れる。
- 2026-09-02 中野区の原因を追った。探索結果は 2026-08-28 から変わっていない。**候補0位（スコア25・775字の年齢別目次）が、4位（スコア21・9,800字の本文 `jidoteate.html`）より上に来ている。** `pick_page` は上位から1本採るので本文に届かない。§4-7b と同じ筋（METHOD §4-4b）。直すと `read_breadth` が変わり72組の測り直しになるので、**先にどのくらいの組で起きているかを測るべき（未着手）**。
- 2026-09-03 起点ページの食い違いを測る道具を作った（`analysis/check_pick.py`）。**最初の版は間違っていた。**「選ばれたページより桁違いに長い候補があるか」だけで数えて27/92組と出したが、**長い候補の中身が手続きと無関係だった**（つどいの場一覧・保育施設一覧・はてなブックマーク・読み上げ代行サービス）。数を報告する前に上位8件のURLを開いたから気づいた。手続きらしさの点で絞って **27組 → 4組（下限）**。誤りの実物は `analysis/out/known-wrong/pick_vs_length_length-only.json`。
- 2026-09-03 **起点を差し替えて点がどう動くか4組で実験した（`--page-url`・実験専用）。方向が定まらなかった。** 中野区 児童手当 20→80（+60）／中央区 児童手当 80→80（±0）／葛飾区 粗大ごみ 60→40（−20）／渋谷区 粗大ごみ 60→**0**（−60）。**「本文が長い方を選べばよい」は成り立たない。** 薄い入口ページの方が良いことがある（渋谷区の 5,421字は手数料一覧17,750字より薄いが、辿れるリンクを持つ入口だった）。
- 2026-09-03 **起点の選び方だけで点が最大60点動く。** 中野区の20点は「区が書いていない」ではなく**こちらが本文に届いていない**結果だった。だが直し方は分かっていない（`score_link` を本文量で置き換えると4組中2組で悪化する）。**直していない。**
- 2026-09-03 **`link_filter` が壊れていた。** `page_host in href` は**URL全体への部分一致**で、区のホスト名を中に含むだけの別ホストを通していた（`b.hatena.ne.jp/entry/https://www.city...`・`translation2.j-server.com/?url=...`・`twitter.com/share?url=...`）。候補2,102本中154本が別ホスト、31組にまたがる。直した（`test_link_filter.py` 10件）。
- 2026-09-03 **METHOD §4-3 が誤りだった。**「同一ホストしか選べないので除外された側は見えない」「粗大ごみで外部の申込サイトを使う区がある可能性は残る」と書いていたが、制限が効いていなかったので**既に読んでいた**。追従で12件（`sodai-web.jp` / `ecolife.e-tumo.jp` / `sodai-nerima.jp` / `ota-sodai.com` / `shinjuku-sodai.com` / `ttzk.graffer.jp`）。**判定は正しい側に転んでいたが、設計どおりではなかった。**
- 2026-09-03 直した判定で既存候補を篩い直すと111本落ちるが、**起点ページが変わる組は0**だった。ただし再探索は候補の集め方自体を変えるので同じとは限らない。**再探索はしていない。公開中のデータは直す前の探索結果で作られている。**
- 2026-09-03 **同じ読み違いを1日に2回した。積み上げブランチの作業ツリーを見て「止まっている」と誤判定した。** ①`status.py` が「見張りが3.7日前・止まっている」と出した → `git fetch origin main` したら0.7日前で動いていた（手元の `origin/main` の**参照自体**が古かった。`_from_main` は枝の作業ツリー対策で、参照の鮮度までは見ていなかった）。②`web/data/history/site-status.jsonl` が6行・8/22で止まって見えた → main には16行あり毎日追記されていた。**毎日のワークフローが書くファイルは、必ず `origin/main` から読む。**
- 2026-09-03 `status.py` に参照の古さを出すようにした（`ref_age_days` / `maybe_unfetched`）。止まって見えるとき、**先に `git fetch` を促す**。ネットに出ないのでどちらかは判定できないため、両方を候補として出す。
- 2026-09-03 **状態の履歴を残すようにした**（`analysis/status.py --record` → `web/data/history/status.jsonl`・1日1行）。これまで `status.py` は毎セッション走るが**読むだけで記録していなかった**ので、「いつ条件が崩れたか」「読めない底がいつ増えたか」を後から言えなかった。毎日の `check-pages.yml` に組み込んだ（LLMもネットも使わない）。数だけ残す（区の名前もURLも持たない）。
- 2026-09-04 **`status.py` に「⑥ 配信」を足した。これが無かったせいで「公開データを更新した」と8日間言い続けた。** 更新していたのは枝の上のファイルで、住民が見る画面（GitHub Pages・main から配信）は 2026-08-17 のまま・`model_version` は空欄だった。住民 59.6/45.7/39.6 に対し手元 63.9/51.3/53.9、未マージ49コミット。**比べる相手は手元ではなく main。** ネットには出ない（`git show origin/main:` を読むだけ）。「次にやること」の先頭に出す（他に埋もれるとまた見落とす）。
- 2026-09-04 現状把握の画面を Claude の中に作るのをやめた。**`python3 analysis/status.py` 一発で足りるべきもの**で、足りていなかったのは⑥だけだった。図（archify）は一度作ったが、数字を手で書き写すスナップショットなので継続的な現状把握には向かない。継続的に見せるなら `web/`（既に11ページある公開サイト）に置く — ただし**main にマージされるまでそれも住民には出ない**。
- 2026-09-04 **CI が8日間ずっと赤だったのに気づいていなかった。** 手元で `python3 -m unittest discover` を**リポジトリ直下でしか回していなかった**ため。CI は `test_*.py` のあるディレクトリを全部探して1つずつ回す（`./analysis` `./crawler` `./experiment` `./extractor` `./gennai_app` `./scorer`）。落ちていたのは `./analysis` だけで、直下の465件はずっと緑だった。**「テストが通る」の意味が、私と CI で違っていた。**
- 2026-09-04 原因① `analysis/failure_distribution.py` が `experiment/out/*.json` を全部「反復実験の記録」と見なしていた。#158 が別種の記録（`cold-ask` / `fee-ask` / `stale-ask` — `results` ではなく `rows` を持つ）を足したので落ちた。**黙って飛ばさず、飛ばしたファイル名を出す**ようにした（数だけ合って中身を読んでいない状態を作らないため）。
- 2026-09-04 原因② 実データを固定するテスト3件が、**固定していた欠陥が直った**ことで落ちていた（「実測時刻は全件不明」「measured_on は全行空欄」）。数字を書き換えるのではなく、守りたかった不変条件に書き直した ——**無い実測時刻を書き出し時刻で埋めない**。`recording_status != "recorded"` の行だけが空になる、という形。
- 2026-09-04 原因③ ゴールデン `test_既存データの再分類値を固定する` の値を更新。**動いた向きが仕事の中身そのもの**: fact_failures 157→138 / not_retrieved 12→3（見に行っていなかったものが読めた）/ fact_missing 134→121（「書いていない」が実は書いてあった）/ 契約違反 2→0。**測り直したときは値を更新してよいが、何がどう動いたかをテストに書き残す**規則をコメントで固定した。
- 2026-09-04 原因④ `web/data/index.json`（各データの sha256）が実体とズレていた。`scores-*.json` を作り直したのに目次を作り直していなかった。`analysis/export_data_index.py` で解消。ワークフローのコメントが警告していた通りのことを踏んだ。
