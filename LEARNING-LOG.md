# LEARNING LOG — 東京エージェント・レディネス調査

作りながら分かったことを書き足していく。日付＋やったこと＋分かったこと。

---

## 2026-07-20 Phase 1（パイロット3自治体で一周）

**やったこと**: 練馬・江戸川・八王子の公式サイトをクロールし、転入届の4項目をAIに抽出させ、
人手の正解と突合して採点。ダッシュボードまで通した。

**実測で出たこと**

- **3自治体とも、転入届の手数料がサイトに書かれていない**。実際は無料なのに、
  エージェントは「記載なし」としか答えられない。これは全自治体に広げたとき記事の見出しになる
- **構造化データ（JSON-LD）は3自治体とも無し**。機械可読性が全員10/20で頭打ち
- 手続きページへの到達は 練馬2ホップ / 江戸川3ホップ / 八王子3ホップ。
  八王子は「転入届」ページが目次だけで中身がなく、さらにリンク先を開く必要があった

**設計として効いたこと**

- 取得（crawler）と読解（extractor）を分けたので、抽出プロンプトを何度直しても
  自治体サイトに再アクセスしていない。生HTMLがキャッシュに残っているため
- 採点（scorer）もネットワークに触らないので、採点基準の試行錯誤が安全にできる

**つまずいたこと**

- 採点器のバグ: ゴールデンCSVの `note` 列（ページの読みにくさを人間向けに書いた注記）を
  採点プロンプトに「補足」として渡していた。注記の内容まで正解要件として扱われ、
  八王子が不当に減点されていた。→ note は渡さないように修正
- 修正後も**判定がぶれる**。同じ抽出結果を2回採点して八王子が70点と80点になった。
  「部分正解」の線引きが実行ごとに揺れる。ここが Phase 2 の本題

**分かっていないこと**: なぜぶれるのか。どうすれば安定するのか。他ではどう検証しているのか

---

## 2026-07-21 ぶれを数字にした／二重計上バグを見つけた

**やったこと**: 先行事例を調べてから、同じ抽出結果を5回採点してぶれ幅を測った。

**事例から分かったこと**

- 採点のぶれは**既知の問題**だった。LLMに採点させると判定順序で結果が変わる「position bias」が
  知られていて、判定器によっては25〜50%がひっくり返る
- 目標も分かった。GPT-4の人間との一致率は80%超で、これは**人間同士の一致率（約81%）と同水準**。
  100%を目指す対象ではなく、人間同士のばらつきが天井
- llms.txt は採点に使わないほうがいい。導入は8.8倍に増えたが**97%のファイルがリクエストゼロ**、
  日本の自治体の採用例はゼロ。加点条件にすると調査の信頼性を落とす

**実測**

```
ぶれ率 22%（LLM判定9件中2件）
江戸川区 70〜85点（幅15） / 八王子市 80〜85点（幅5） / 練馬区 90点（幅0）
割れたのは2件とも「正解 ↔ 部分正解」の境目だけ
```

練馬が幅0だったのが手がかりになった。練馬は4項目すべて「正解」で、判定に迷う余地がない。
**ぶれているのは判定基準の曖昧な場所だけ**で、採点器が全体的に不安定なわけではない。

**そこから見つけたバグ**

江戸川は割れた項目が1つなのに合計が15点動いた。数字が合わないので追ったら、
「窓口オンライン可否」の**同じ判定が2箇所で使われていた**。

```
正解 → 部分正解 に落ちると
  抽出正確性  10点 → 5点  （−5）
  オンライン明示 20点 → 10点 （−10）
                           合計 −15
```

1つの判定が100点中30点を動かす増幅器になっていた。ぶれ率22%自体は許容範囲でも、
これがあると順位が壊れる。**ぶれを測ったから見つかったバグで、測らなければ気づかなかった。**

**直し方と、そこで踏んだ失敗**

オンライン明示の点は、抽出時に一度だけ観測する `online_clarity`（明記/曖昧/記載なし）から
機械的に決めるようにした。観測を上流で固定すれば、同じ抽出結果からは必ず同じ点が出る。
観測結果自体は妥当だった（八王子=曖昧。「手続によってはオンライン申請に対応」としか
書いておらず、転入届が完結するか判断できない）。

**ただし、抽出プロンプトに `online_clarity` の説明を足したら、4項目の抽出結果まで変わった。**
江戸川の必要書類と八王子の窓口オンライン可否が「見つからず（曖昧）」に落ちた。
「曖昧」という言葉を新しく教えたことで、他の項目でもそれを選びやすくなった疑いが濃い。

**教訓**: プロンプトは1か所直したつもりでも、全体の挙動が変わる。変更は1つに絞って前後を比べる。
それと、原因が2つ混ざった状態で検証を重ねても何も分からないので、混ざったら止めて切り分ける。

**分かっていないこと**: 「正解/部分正解」の境目をどう安定させるか。二値にすると解像度が落ちる

---

## 2026-07-22 採点器を作り直した／23区に広げ始めた

**やったこと**: 採点を「必須要素チェック方式」に作り直した（判定器は要素ごとに yes/no を
答えるだけ。点は機械が計算）。ゴールデンの全28スロットをワークフローで監査して13件書き直し、
判定ルールを厳密側に固定。合計4本の測定で効果を確認してから、23区のクロールと読解に進んだ。

**実測で出たこと**

- 採点のぶれは 当初±15点 → **最大±2点** まで縮んだ。ゼロにはならない。
  残る揺れは「エージェントが条件を『該当者のみ』と圧縮した答えの意味照合」という本物の境界事例
- **23区中23区で転入届ページに到達**。ただし北区は旧ドメインが移行案内だけになっていて、
  古いURLからだと全リンク404 = 古い知識のAIは北区に辿り着けない（調査の発見）
- 読解で早くも差が出た: 港区は4項目全部読めた唯一の区。中央・台東・墨田・荒川は
  AIが電子申請案内や入口ページに吸い込まれてほぼ読めず
- 手数料はどの区もサイトに書いていない（Phase 1 の傾向が23区でも再現）

**教訓**

- **正確さと安定性は別物**。ゴールデンを原典どおりの条件付き文言に直したら、採点は正しく
  なったのにぶれはむしろ増えた（答えの圧縮癖との突合が新しい境界事例になった）
- **1スロット1事項**。1つの採点要素に2つの事実を詰めると判定が割れる。実測で3件とも該当
- **±2点は測定誤差として明記して出す**。ゼロにする往復を続けるより、誤差込みで
  「2点差以内は同順位帯」と書く方が調査として正直で強い
- **本人が評価できない確認を渡さない**。13行の突合表「合ってるか見て」は答えられない。
  機械照合(13/13)と実サイト照合で裏を取り、結果の数字だけ報告する形に変えた

**分かっていないこと**: 読めなかった区（中央・台東・墨田・荒川）が「サイトが悪い」のか
「探索の点数付けが悪い」のか。ゴールデンを20区に増やすときの品質をどう機械で担保するか
