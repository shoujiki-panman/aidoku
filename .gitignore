# 生HTMLキャッシュは再現用に残すが、リポジトリには入れない（自治体サイトの著作物のため）
crawler/cache/

__pycache__/
*.pyc
.DS_Store
.venv/

# wrangler dev がローカルに作る状態（KVの中身・証明書）。手元だけのもの
.wrangler/

# evidence check の既存出力コピーは再生成できるため入れない。集計JSONは追跡する。
analysis/out/evidence-checked/

# Lint / 重複検出のキャッシュ
.ruff_cache/
report/

# クラウド同期ソフトが競合したときに作る複製（「journey 2.js」「archive 2.json」）。
# 本体を古い版に巻き戻す事故のもとなので、git には載せない。
* [2-9].*


# 毎朝の差分（analysis/daily_diff.py）。区のサイトの文をそのまま含むので公開しない（#100 と同じ扱い）。
# 前回の取得を残しておく場所も、生HTMLなので同じ。
analysis/out/daily/
crawler/cache-daily/
